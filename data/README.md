# Private Dataset Placeholder

The benchmark dataset is private and not included in the repository.

Expected files:
- `data/food_truck_ops_cases.jsonl` (cases)
- `data/predictions_example.jsonl` (optional local sample predictions)

See `tools/case_template.json` for the case schema.
